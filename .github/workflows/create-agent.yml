name: Create Agent

on:
  workflow_dispatch:
    inputs:
      agent_name:
        description: "Agent name in kebab-case (e.g., my-agent)"
        required: true
        type: string
      model:
        description: "Model name for the agent"
        required: false
        default: "gpt-4o"
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  scaffold-agent:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      AGENT_NAME: ${{ inputs.agent_name }}
      AGENT_MODEL: ${{ inputs.model }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Validate agent name
        run: |
          python -c "
          import os, sys
          sys.path.insert(0, 'scripts')
          from create_agent import validate_name
          name = os.environ['AGENT_NAME']
          err = validate_name(name)
          if err:
              print(f'[validate] \u2717 {err}', file=sys.stderr)
              sys.exit(1)
          print(f'[validate] Agent name {name!r} is valid')
          "

      - name: Run scaffolding script
        run: python scripts/create_agent.py --name "$AGENT_NAME" --model "$AGENT_MODEL"

      - name: Create branch and commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "scaffold/$AGENT_NAME"
          git add .
          git commit -m "feat: scaffold agent $AGENT_NAME"
          git push origin "scaffold/$AGENT_NAME"

      - name: Create pull request
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create \
            --title "feat: add agent $AGENT_NAME" \
            --body "## New Agent: $AGENT_NAME

          Scaffolded via GitHub Actions workflow.

          ### Generated Files
          - \`agents/$AGENT_NAME/\` — agent code
          - \`tests/$AGENT_NAME/\` — test stubs
          - \`agents/registry.py\` — updated with new entry

          ### Next Steps
          1. Review and customise \`instructions.md\`
          2. Add custom tools
          3. Merge when ready" \
            --base main
